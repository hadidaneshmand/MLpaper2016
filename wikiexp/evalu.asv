a1 = A(:,:,1,1); 
a2 = A_hat(:,:,1,1);
b1 = A(:,:,1,2); 
b2 = A_hat(:,:,1,2);
t = 0.2;
R(1,1) = sum(a1(:)>0 & a2(:)>t)/sum(a1(:)>0);
R(1,2) = sum(a1(:)>0 & a2(:)>t)/sum(a2(:)>t);
R(2,1) = sum(b1(:)>0 & b2(:)>t)/sum(b1(:)>0);
R(2,2) = sum(b1(:)>0 & b2(:)>t)/sum(b2(:)>t);